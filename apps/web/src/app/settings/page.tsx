import { SettingsView } from '@/components/settings/settings-view';

export const metadata = { title: 'Settings — Sanjeevani' };

export default function SettingsPage() {
  return <SettingsView />;
}
